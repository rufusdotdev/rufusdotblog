---
layout: layouts/post.njk
title: On keeping notes
deck: A second placeholder, mostly to check that the index page sorts newest-first correctly.
date: 2026-06-26
---

Posts are just markdown files in `/src/posts/`, named `YYYY-MM-DD-slug.md`. The filename date isn't what drives sort order — the `date` field in the front matter does. Eleventy's collection is built from that field and reversed so the newest post lands first on the home page.

Add images, code, or anything else markdown supports. This one's here purely to prove there's more than one entry in the list.
